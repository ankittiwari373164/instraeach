[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "pip install instagrapi Pillow --break-system-packages || pip install instagrapi Pillow"
]

[start]
cmd = "node api/server.js"
