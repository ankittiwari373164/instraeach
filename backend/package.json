{
  "name": "instraeach-backend",
  "version": "1.0.0",
  "description": "InstaReach DM Automation Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && (pip3 install instagrapi requests --quiet --break-system-packages || pip3 install instagrapi requests --quiet || python3 -m pip install instagrapi requests --quiet || echo \"pip install failed - will retry at runtime\")"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "sql.js": "^1.10.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^16.4.5",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}